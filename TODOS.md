# TODOS — AI 宠物数字分身

## TODO 1：AI 图像异步队列升级

**What:** 当用户量超过 5000 时，将 AI 图像风格化从同步改为异步队列处理。

**Why:** 同步处理在 MVP 阶段可行，但高并发时大量用户同时等待会打爆 AI API 并发限制，导致超时率上升。

**Pros:** 彻底解决并发问题；用户体验从"等待"变为"通知到达"；API 限流可控。

**Cons:** 需要引入消息队列（阿里云 MNS 或 Redis Queue）；需要微信订阅消息授权；代码量增加约 30%。

**Context:** 目前 MVP 使用同步调用（wx.request 超时 60 秒）。Stability AI / 通义万相 P95 延迟约 15-25 秒，MVP 阶段用 loading 动画覆盖。当日活 >1000（约 5000 注册用户）时评估改造。

**Depends on:** 用户量达到 5000 评估节点

---

## TODO 2：OpenID → UnionID 数据迁移方案

**What:** 在扩展到多个微信平台入口（公众号/H5/App）之前，制定并执行 OpenID 映射 UnionID 的用户数据迁移。

**Why:** 目前只用 OpenID（小程序专属），无法跨平台识别同一用户。如果先扩多端再迁移，用户数据会分裂，无法合并历史宠物数据。

**Pros:** 一次迁移解决多端用户统一问题；为宠物数据跨端同步打基础。

**Cons:** 迁移需要用户重新触发微信授权（获取 UnionID 需公众号绑定）；有少量用户可能迁移失败；需要双写过渡期。

**Context:** 迁移入口：在小程序中引导用户关注公众号，借此获取 UnionID 并建立映射表。users 表已预留 union_id 字段（nullable），迁移时填充。

**Depends on:** 有第二个微信平台入口需求时启动

---

# TODOS — 小米财报 AI 解读器

## TODO 3：v2 — 竞对手对比看板（小米 vs 华为/OPPO）

**What:** Hackathon 后如果继续迭代，加入华为/OPPO 的核心财务数据，做同维度对比看板（营收、手机出货量、毛利率趋势）。

**Why:** 评委在 demo 现场最常见的追问是"小米和竞对手相比怎么样"。目前 demo 只有小米自身数据，无法回答这个问题。加入竞对数据后 AI 能直接对比分析。

**Pros:** 显著提升 demo 的叙事价值；华为/OPPO 同样有港交所/公开财报；JSON schema 已设计好扩展性。

**Cons:** 需要为每个竞对再写一套 parser（或手动录入）；竞对数据可比性问题（定义不完全一致）；demo 范围扩大增加复杂度。

**Context:** 当前 data.json schema 以公司为分组很容易扩展。先提交 hackathon，再评估是否往这个方向做产品化。

**Depends on:** Hackathon 提交后评估
